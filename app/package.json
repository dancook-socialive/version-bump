{
  "name": "app",
  "version": "1.7.1",
  "private": true,
  "scripts": {
    "build": "echo 'build app'",
    "start": "yarn node ./index.js"
  },
  "dependencies": {
    "@version-bump/shared": "1.0.0"
  }
}
